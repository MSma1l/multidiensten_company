# Frontend dev config for `npm run dev`.
# Base URL of the FastAPI backend the Contact form posts to.
# This file is only loaded in Vite's "development" mode, so it does not affect
# the test run (vitest uses mode "test").
VITE_API_URL=http://localhost:8000
